export default function Laptop({ laptop }) {
  const { laptop_name, price } = laptop

  return <section id="laptopList">
    {
      listLaptops.map(
        (laptop_name, price) => (Laptop

        )
      )
    }
  </section>
}